import gpu
import interop
import luce_ui.layout as layout
import input
import luce_ui.cursor as cursor
pub type Cursor = input.Cursor
pub type CursorSource = cursor.CursorSource
import luce_ui.painter as painter
import luce_ui.result as result
import luce_ui.widgets.text as text
import luce_ui.widgets.button as button
import luce_ui.widgets.slider as slider
import luce_ui.widgets.shelf as shelf
import luce_ui.widgets.rule as rule
import luce_ui.widgets.checkbox as checkbox
import luce_ui.widgets.dialog as dialog
import luce_ui.widgets.hsb_field as hsb_field
import luce_ui.widgets.shortcut_field as shortcut_field
import luce_ui.widgets.shaded_field as shaded_field
import luce_ui.widgets.text_field as text_field
import luce_ui.widgets.number_field as number_field
import luce_ui.widgets.curve_editor as curve_editor
import luce_ui.widgets.spacer as spacer
import luce_ui.widgets.viewport as viewport
import luce_ui.widgets.vstack as vstack
import luce_ui.widgets.hstack as hstack

pub let invalid: ErrorCode = result.invalid
pub type Widget = layout.Widget
pub type Layout = layout.Layout
pub type Size = layout.Size
pub type Point = layout.Point
pub type Constraints = layout.Constraints
pub type Event = layout.Event
pub type Axis = layout.Axis
pub type Alignment = layout.Alignment
pub type PopupPlacement = layout.PopupPlacement
pub type Rect = layout.Rect
pub type Painter = painter.Painter
import luce_ui.raster as raster
pub type Raster = raster.Raster
pub let raster_type = raster.raster_type
import luce_ui.image as image
pub type Image = image.Image
pub let image_type = image.image_type
pub type Text = text.Text
pub type Button = button.Button
pub type Slider = slider.Slider
pub type Shelf = shelf.Shelf
pub type Rule = rule.Rule
pub type Checkbox = checkbox.Checkbox
pub type Dialog = dialog.Dialog
pub type HsbField = hsb_field.HsbField
pub type HueStrip = hsb_field.HueStrip
pub type ShortcutField = shortcut_field.ShortcutField
pub type ShadedField = shaded_field.ShadedField
pub type TextField = text_field.TextField
pub type NumberField = number_field.NumberField
pub type ButtonStyle = button.ButtonStyle
pub type CurveEditor = curve_editor.CurveEditor
pub type Spacer = spacer.Spacer
pub type Viewport = viewport.Viewport
pub type VStack = vstack.VStack
pub type HStack = hstack.HStack

import luce_ui.application as application
pub type Application = application.Application

import luce_ui.font as font
pub type Font = font.Font
pub let font_type = font.font_type
pub let busy: ErrorCode = result.busy
pub let button_type = button.button_type
pub let slider_type = slider.slider_type
pub let shelf_type = shelf.shelf_type
pub let rule_type = rule.rule_type
pub let checkbox_type = checkbox.checkbox_type
pub let dialog_type = dialog.dialog_type
pub let hsb_field_type = hsb_field.hsb_field_type
pub let hue_strip_type = hsb_field.hue_strip_type
pub let shortcut_field_type = shortcut_field.shortcut_field_type
pub let shaded_field_type = shaded_field.shaded_field_type
pub let text_field_type = text_field.text_field_type
pub let number_field_type = number_field.number_field_type
pub let curve_editor_type = curve_editor.curve_editor_type
pub let text_type = text.text_type
pub let spacer_type = spacer.spacer_type
pub let viewport_type = viewport.viewport_type
pub let vstack_type = vstack.vstack_type
pub let hstack_type = hstack.hstack_type
pub let application_type = application.application_type
pub let painter_type = painter.painter_type
pub let layout_type = layout.layout_type

import luce_ui.widgets.text_editor as text_editor
import luce_ui.text.style as style
pub type TextEditor = text_editor.TextEditor
pub type Highlight = style.Highlight
pub type EditorTheme = style.EditorTheme
pub let text_editor_type = text_editor.text_editor_type

import luce_ui.widgets.list_view as list_view
pub type ListView = list_view.ListView
pub type ListItem = list_view.ListItem
pub let list_view_type = list_view.list_view_type

import luce_ui.widgets.table_view as table_view
pub type TableView = table_view.TableView
pub type TableColumn = table_view.TableColumn
pub type TableCell = table_view.TableCell
pub type CellAlign = table_view.CellAlign
pub let table_view_type = table_view.table_view_type

import luce_ui.text.document as document
pub type TextChange = document.TextChange

import luce_ui.theme as theme
pub type Theme = theme.Theme

import luce_ui.command as command
pub type Command = command.Command
pub type Shortcut = command.Shortcut
pub let command_type = command.command_type
import luce_ui.widgets.toolbar as toolbar
pub type Toolbar = toolbar.Toolbar
pub let toolbar_type = toolbar.toolbar_type

import luce_ui.widgets.menu as menu
pub type Menu = menu.Menu
pub let menu_type = menu.menu_type

import luce_ui.widgets.pane as pane
pub type Pane = pane.Pane
pub let pane_type = pane.pane_type
import luce_ui.widgets.split_view as split_view
pub type SplitView = split_view.SplitView
pub let split_view_type = split_view.split_view_type

import luce_ui.text.folding as folding
pub type FoldRange = folding.FoldRange

import luce_ui.widgets.context_menu as context_menu
pub type ContextMenu = context_menu.ContextMenu
pub let context_menu_type = context_menu.context_menu_type
import luce_ui.widgets.command_palette as command_palette
pub type CommandPalette = command_palette.CommandPalette
pub let command_palette_type = command_palette.command_palette_type
import luce_ui.widgets.text_prompt as text_prompt
pub type TextPrompt = text_prompt.TextPrompt
pub let text_prompt_type = text_prompt.text_prompt_type

import luce_ui.widgets.confirm_dialog as confirm_dialog
pub type ConfirmDialog = confirm_dialog.ConfirmDialog
pub let confirm_dialog_type = confirm_dialog.confirm_dialog_type

import luce_ui.widgets.color_editor as color_editor
pub type ColorEditor = color_editor.ColorEditor
pub let color_editor_type = color_editor.color_editor_type

import luce_ui.icons as icons
import luce_ui.widgets.icon as icon
import luce_ui.widgets.pane_header as pane_header
pub type IconKind = icons.IconKind

## Draw the luciaos-assets icon called `name` into `bounds` of `target`, in `color`.
pub func draw_icon(target: interop.View[gpu.RenderTarget], name: str, bounds: gpu.Rect, color: gpu.Color) -> !:
    try icons.draw_icon(target, name, bounds, color)

## An sRGB colour from hue (0..360), saturation and brightness (0..1).
pub func hsb_to_rgb(hue: f64, saturation: f64, brightness: f64) -> gpu.Color:
    return hsb_field.hsb_to_rgb(hue, saturation, brightness)

## The hue of an sRGB colour, 0..360 (0 for greys).
pub func hue_of(color: gpu.Color) -> f64:
    var h = 0.0
    var s = 0.0
    var b = 0.0
    hsb_field.rgb_to_hsb(color, &h, &s, &b)
    return h

## The saturation of an sRGB colour, 0..1.
pub func saturation_of(color: gpu.Color) -> f64:
    var h = 0.0
    var s = 0.0
    var b = 0.0
    hsb_field.rgb_to_hsb(color, &h, &s, &b)
    return s

## The brightness of an sRGB colour, 0..1.
pub func brightness_of(color: gpu.Color) -> f64:
    var h = 0.0
    var s = 0.0
    var b = 0.0
    hsb_field.rgb_to_hsb(color, &h, &s, &b)
    return b

## Whether `name` is an icon in the set.
pub func icon_known(name: str) -> bool:
    return icons.known(name)

pub type Icon = icon.Icon
pub let icon_type = icon.icon_type
pub type PaneHeader = pane_header.PaneHeader
pub let pane_header_type = pane_header.pane_header_type

import luce_ui.widgets.panel as panel
import luce_ui.widgets.dstack as dstack
import luce_ui.docking.model as docking_model
import luce_ui.docking.state as docking_state
import luce_ui.arrangement as arrangement
pub type Panel = panel.Panel
pub let panel_type = panel.panel_type
pub type DStack = dstack.DStack
pub let dstack_type = dstack.dstack_type
pub type DockPosition = docking_model.DockPosition
pub type DockRequest = docking_state.DockRequest
pub let dock_request_type = docking_state.dock_request_type
pub type Arrangement = arrangement.Arrangement
