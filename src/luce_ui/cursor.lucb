import input
from luce_ui.geometry import Point

## Optional hit-dependent cursor policy. Coordinates are local to the widget;
## capture keeps a resize/drag cursor active when the pointer crosses its bounds.
## Queries must not change layout, dispatch events or draw.
pub interface CursorSource:
    func cursor(position: Point, captured: bool) -> input.Cursor
